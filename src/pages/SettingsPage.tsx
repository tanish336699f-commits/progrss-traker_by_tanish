import { useTheme } from '@/lib/theme';
import { Moon, Sun, Database, Trash2 } from 'lucide-react';
import { db } from '@/lib/db';

export function SettingsPage() { const { theme, toggleTheme } = useTheme(); const clear = async () => { if (window.confirm('Delete all activities? This cannot be undone.')) { await db.activities.clear(); window.location.reload(); } }; return <div className="max-w-2xl space-y-5"><div className="card"><div className="flex items-center gap-3 mb-4"><div className="icon-box"><Database className="w-5 h-5" /></div><div><h3 className="font-semibold text-gray-900 dark:text-white">Your data</h3><p className="text-sm text-gray-500">Stored privately on this device</p></div></div><p className="text-sm text-gray-600 dark:text-gray-300 leading-relaxed">Progress Tracker is local-first. Your activities, subjects, chapters, skills, and timer state stay in your browser and never leave your device.</p></div><div className="card flex items-center justify-between"><div className="flex items-center gap-3"><div className="icon-box">{theme === 'light' ? <Sun className="w-5 h-5" /> : <Moon className="w-5 h-5" />}</div><div><h3 className="font-semibold text-gray-900 dark:text-white">Appearance</h3><p className="text-sm text-gray-500">Currently using {theme} mode</p></div></div><button onClick={toggleTheme} className="btn-secondary">Switch to {theme === 'light' ? 'dark' : 'light'}</button></div><div className="card border-red-200 dark:border-red-900/50"><div className="flex items-center gap-3"><Trash2 className="w-5 h-5 text-red-500" /><div className="flex-1"><h3 className="font-semibold text-gray-900 dark:text-white">Clear activities</h3><p className="text-sm text-gray-500">Remove all logged learning sessions</p></div><button onClick={clear} className="text-sm font-semibold text-red-600 hover:text-red-700">Clear</button></div></div><p className="text-center text-sm text-gray-400 dark:text-gray-500 pt-2">Made by Tanish Agarwal</p></div>; }
